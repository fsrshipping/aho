const mailbutton = document.getElementById("contact-email");

mailbutton.addEventListener("click", notify);

function notify() {
    alert("Your browser doesn't support this function, please contact your referrant");
}